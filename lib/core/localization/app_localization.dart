import 'dart:ui';

class AppLocalization {
  static const supportedLocales = [Locale('en'), Locale('ar')];
  static const fallbackLocale = Locale('en');
  static const startLocal = Locale('en');
  static const path = "assets/translations";

}
